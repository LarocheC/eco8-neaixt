# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/

# Compiled extensions (rebuilt by `uv sync`)
*.so
*.pyd
*.dylib

# uv-managed virtualenv
.venv/

# Training outputs
cp_*/
generated_files/
generated_files_*/
logs/
**/.quant_cache/
*.log
# Windowed-rework eval outputs (regenerable: deploy/stm32n6/scripts/run_windowed_eval.sh)
deploy/stm32n6/windowed_eval/
# Benchmark working dirs: HF snapshots, ~10 GB of cached enhanced audio, and the
# per-utterance score dumps. Regenerable:
#   python -m benchmarks.enhance && python -m benchmarks.score
# The durable outputs are committed: RESULTS_METRICS.md + benchmarks/summary.json.
benchmarks/out/
benchmarks/smoke/
# Gate-0 probe ONNX (regenerable: deploy/stm32n6/scripts/build_gate0d_probe.py)
deploy/stm32n6/gate0_artifacts/
# Stray deployment ONNX at the repo root (real artifacts live under cp_*/)
/g_best*.onnx

# GSD planning (kept local — commit_docs=false)
.planning/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# HF upload staging
hf_staging/

# ORT / PESQ scratch artifacts
sym_shape_infer_temp.onnx
*.data

# STM deployment artifacts
deploy/stm32n6/cloud/.env

# STM32CubeProgrammer installer download
SetupSTM32CubeProgrammer_linux_64.zip

# STM32CubeCLT installer download
st-stm32cubeclt_*.zip
st-stm32cubeclt_*.sh

# --- RT595 deploy: external SDK symlink + build/generated artifacts ---------------
deploy/rt595/sdk
deploy/rt595/build/
deploy/rt595/host_out/
deploy/rt595/host/__pycache__/
# TFLM source tree + built lib (regenerable: deploy/rt595/tflm/build_rt500.sh)
deploy/rt595/tflm/tflite-micro/
deploy/rt595/tflm/*.a
# ISS scratch builds
deploy/rt595/iss/work/
deploy/rt595/iss/**/*.elf
deploy/rt595/iss/**/*.o
# lane: pytest + accel-map cache
deploy/rt595/lane/**/__pycache__/
deploy/rt595/lane/.pytest_cache/
# lane: git worktree overlays for checkpoints whose model code lives on another branch
.lane/

# --- Licences and vendor installers (never redistributable) -----------------------
# Cadence licence — node-locked, expires 2027-07-28. Obtain per deploy/rt595/tflm/README.md
RT500SDK.lic
LinkServer_*.deb.bin
nxp_rt500_*_linux.tgz


# --- Handover tarballs (large, regenerable from the branch they were cut from) -----
*-handover-*.tar.gz
*_handover_*.tar.gz
# --- Paper scratch: regenerable quantisation graphs -------------------------------
paper/data/tmp_quant/
